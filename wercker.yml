box: darron/kvexpress-test
build:
  steps:
    # Build the project
    - script:
        name: go build
        code: |
          make deps
          make build

    # Test the project
    - script:
        name: go test
        code: |
          testing/wercker.sh
