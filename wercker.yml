box: darron/consul-wercker
build:
  steps:
    # Build the project
    - script:
        name: go build
        code: |
          make deps
          make

    # Test the project
    - script:
        name: go test
        code: |
          make test
